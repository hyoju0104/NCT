<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lec.spring.repository.UserRepository">

    <select id="findByUsername" resultType="com.lec.spring.domain.User">
        SELECT
            id, auth_id AS authId, plan_id AS planId, username, password,
            name, phone_num AS phoneNum, email, address, point, provider,
            provider_id AS providerId, signed_at AS signedAt,
            paid_at AS paidAt, status_plan AS statusPlan,
            status_account AS statusAccount, rental_cnt AS rentalCnt
        FROM user
        WHERE username = #{username}
    </select>

    <select id="findById" resultType="com.lec.spring.domain.User">
        SELECT
            id, auth_id AS authId, plan_id AS planId, username, password,
            name, phone_num AS phoneNum, email, address, point, provider,
            provider_id AS providerId, signed_at AS signedAt,
            paid_at AS paidAt, status_plan AS statusPlan,
            status_account AS statusAccount, rental_cnt AS rentalCnt
        FROM user
        WHERE id = #{id}
    </select>

    <insert id="save" parameterType="com.lec.spring.domain.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (
            username, password, name, email, phone_num, address,
            auth_id, status_plan, status_account,
            point, rental_cnt
        ) VALUES (
                     #{username}, #{password}, #{name}, #{email}, #{phoneNum}, #{address},
                     #{authId}, #{statusPlan}, #{statusAccount},
                     #{point}, #{rentalCnt}
                 )
    </insert>


</mapper>
